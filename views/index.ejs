<%- include('header') %>

<div>
    <h3>Welcome <%= name %>!</h3>
    <% if (authen) { %>
        <nav>
            <a class="button" id="special" href="/add">Add trading card!</a></h5> |
            <a href="/member?id=<%= authen %>">Your cards</a>
        </nav>
    <% } else { %>
        <a class="button" href="/login">Login</a>
    <% } %>
    <div>
        <h4 class="center">Sets</h4>
        <table class="center">
            <thead>
                <tr>
                    <th scope="col">Name</th>
                    <th scope="col">Cards</th>
                    <th scope="col">Date released</th>
                </tr>
            </thead>
            <tbody>
                <% sets.forEach((set)=> {
                    const date = {
                        year: 'numeric',
                        month: 'long',
                        day: 'numeric'
                    }; %>
                    <tr>
                        <td><a href="/set?id=<%= set.set_id %>"><%= set.set_name %></a></td>
                        <td><%= set.set_num_cards %></td>
                        <td><%= set.set_date.toLocaleDateString('en-GB', date) %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
    <div>
        <h4 class="center">Members</h4>
        <table class="center">
            <thead></thead>
            <tbody>
                <% users.forEach((user) => { %>
                    <tr>
                        <td><a href="/member?id=<%= user.user_id %>"><%= user.username %></a></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>    
</div>
<%- include('footer') %>